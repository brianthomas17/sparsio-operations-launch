

## Fix CNAME File

Update the `CNAME` file from `sparsiodesign.com` to `sparsiosystems.com` to match the new domain.

### Change
- **`CNAME`**: Replace `sparsiodesign.com` with `sparsiosystems.com`

